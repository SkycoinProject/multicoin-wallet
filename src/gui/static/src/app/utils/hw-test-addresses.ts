/**
 * Returns a map with known mainnet addresses (as keys) and the corresponding testnet
 * equivalents (as values). This is for being able to work with testnet and regtest
 * nodes when it is only possible to get mainnet addresses.
 */
export function getTestAddressesMap(): Map<string, string> {
  const response = new Map<string, string>();

  // Populate the map here. You must calculate the tesnet equivalents before doing this.
  // Please do not include the changes in the repo.

  return response;
}
